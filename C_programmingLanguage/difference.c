#include<stdio.h>
int main()
{
    int num1, num2, Diff;
    printf("Enter your two number: ");
    scanf("%d %d", &num1, &num2);

    Diff = num1 - num2;

    printf("Your diffrence is: %d", Diff);
}